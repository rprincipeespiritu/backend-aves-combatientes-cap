{
  "name": "backend-aves-combatientes",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "dependencies": {
    "@cap-js/postgres": "^2.1.3",
    "@sap/cds": "^9",
    "@sap/cds-dk": "^9.8.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^4",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2",
    "sqlite3": "^5.1.7"
  },
  "scripts": {
    "start": "npx cds deploy --profile pg && npx cds serve",
    "deploy": "npx cds deploy --profile pg",
    "watch": "cds watch",
    "build": "cds build"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "postgres"
      },
      "auth": {
        "kind": "dummy"
      }
    }
  }
}
