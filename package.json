{
  "name": "backend-aves-combatientes",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "dependencies": {
    "@cap-js/postgres": "^2.1.3",
    "@sap/cds": "^9",
    "@sap/xssec": "^4",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^4",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "@sap/cds-dk": "^9.8.0",
    "@cap-js/sqlite": "^2",
    "sqlite3": "^5.1.7"
  },
  "scripts": {
    "start": "cds-serve",
    "watch": "cds watch",
    "build": "cds build",
    "deploy:pg": "cds deploy --to postgres",
    "deploy:railway": "node deploy.js"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "postgres"
      }
    }
  }
}
